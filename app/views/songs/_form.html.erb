<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= f.label :artist_name %>
  <%= display_name(@song, @artist) %>
  <%= artist_select(@song, @artist) %>
  <br />

  <%= f.submit %>
<% end %>
